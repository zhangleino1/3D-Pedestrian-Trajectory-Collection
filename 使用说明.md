# 3D Pedestrian Trajectory Collection - 安装与使用指南

## 目录
- [快速开始（5分钟）](#快速开始5分钟)
- [详细安装说明](#详细安装说明)
- [数据准备](#数据准备)
- [使用说明](#使用说明)
- [输出结果说明](#输出结果说明)
- [常见问题](#常见问题)
- [性能优化建议](#性能优化建议)

---

## 快速开始（5分钟）

### 第一步：安装 Anaconda

如果已安装 Anaconda，请跳过此步骤。

**下载地址**: https://www.anaconda.com/download

- 选择 Python 3.11 版本
- Windows 用户安装后使用 **Anaconda Prompt** 运行以下命令

### 第二步：创建并激活环境

```bash
# 创建 Python 3.11 环境
conda create -n trajectory python=3.11 -y

# 激活环境
conda activate trajectory
```

### 第三步：安装依赖

**Anaconda 用户（推荐）**：
```bash
# Anaconda 已预装大部分科学计算包，通常只需安装 numpy-quaternion
pip install numpy-quaternion

# 或使用 requirements.txt 一次性安装所有依赖
pip install -r requirements.txt
```

**验证安装**：
```bash
python -c "import numpy; import pandas; import quaternion; import scipy; import matplotlib; print('✓ 所有依赖安装成功！')"
```

### 第四步：处理数据

```bash
# 1. 转换原始数据为 CSV
python to_csv.py

# 2. 生成统计摘要
python summary.py

# 3. 可视化轨迹（可选）
python trace_view.py
```

**完成！** 🎉 现在您可以在 `csv/`、`processed/` 和 `image/` 目录中查看结果。

---

## 详细安装说明

### 环境要求

- **操作系统**: Windows / Linux / macOS
- **Python 版本**: Python 3.11
- **硬盘空间**: 至少 5GB（用于 Anaconda 和依赖）
- **推荐配置**: Anaconda Python 发行版

---

### Anaconda 安装

#### 1. 下载并安装 Anaconda

- 访问 [Anaconda 官网](https://www.anaconda.com/download)
- 下载适合你操作系统的安装包（建议选择 Python 3.11 版本）
- 运行安装程序并按照提示完成安装

**安装建议**:
- Windows 用户: 建议勾选 "Add Anaconda to PATH environment variable"（添加到系统路径）
- 安装过程约需 5-10 分钟
- Anaconda 自带 NumPy、Pandas、SciPy、Matplotlib 等科学计算包，无需额外安装基础库

#### 2. 验证 Anaconda 安装

打开终端（Windows 使用 **Anaconda Prompt** 或 **Anaconda PowerShell Prompt**），运行：

```bash
conda --version
python --version
```

如果分别显示版本号（如 `conda 23.7.4` 和 `Python 3.11.x`），说明安装成功。

---

### 环境搭建


```

#### 2. 创建 Conda 虚拟环境

```bash
# 创建名为 trajectory 的 Python 3.11 环境
conda create -n trajectory python=3.11 -y

# 激活环境
conda activate trajectory
```

**说明**:
- `-n trajectory`: 指定环境名称为 `trajectory`
- `python=3.11`: 指定 Python 版本为 3.11
- `-y`: 自动确认安装

#### 3. 验证环境激活

激活后，命令行提示符前应显示 `(trajectory)`：

```bash
(trajectory) D:\work\researchcode\3D-Pedestrian-Trajectory-Collection>
```

---

### 依赖安装

#### 方法一: 一键安装（推荐 - Anaconda 用户）

**对于 Anaconda 用户，大部分依赖已预装，通常只需安装 numpy-quaternion：**

```bash
# 确保已激活 trajectory 环境
conda activate trajectory

# 检查预装的包（可选）
conda list | grep -E "numpy|pandas|scipy|matplotlib"

# 仅安装缺失的包（通常只需这个）
pip install numpy-quaternion

# 或者使用 requirements.txt 一次性检查并安装所有依赖
pip install -r requirements.txt
```


#### 验证安装

运行以下 Python 代码验证所有依赖是否正确安装：

```bash
python -c "import numpy; import pandas; import quaternion; import scipy; import matplotlib; print('所有依赖安装成功！')"
```

如果输出 `所有依赖安装成功！`，说明环境配置完成。

---

## 数据准备

### 1. 准备原始数据

将原始传感器数据放入 `raw_data/` 目录，每次采集的数据应包含在一个以时间戳命名的子目录中，例如：

```
raw_data/
├── 20240829_225513/
│   ├── pose.txt          # 位置和姿态数据（必需）
│   ├── acce.txt          # 加速度计数据
│   ├── gyro.txt          # 陀螺仪数据
│   ├── linacce.txt       # 线性加速度数据
│   ├── gravity.txt       # 重力向量数据
│   ├── magnet.txt        # 磁力计数据
│   └── orientation.txt   # 设备朝向数据
├── 20240829_225558/
│   └── ...
```

**重要说明**:
- `pose.txt` 是必需的，格式为 8 列数据（时间戳 + 3D 位置 + 四元数）
- 其他传感器文件如果缺失，程序会用零值填充（会有警告提示）

### 2. 确保目录结构

```bash
# Windows 用户（PowerShell 或 CMD）
mkdir raw_data
mkdir csv
mkdir processed
mkdir image

# Linux/macOS 用户
mkdir -p raw_data csv processed image
```

---

## 使用说明

### 完整处理流程

#### 第一步: 转换原始数据为 CSV

```bash
# 激活环境
conda activate trajectory

# 使用默认参数处理（采样率 180Hz，跳过前后各 60 个样本）
python to_csv.py

# 或使用自定义参数
python to_csv.py --input ./raw_data --output ./csv --sampling_rate 180 --skip_front 60 --skip_end 60
```

**参数说明**:
- `--input`: 原始数据目录（默认: `./raw_data`）
- `--output`: 输出 CSV 目录（默认: `./csv`）
- `--sampling_rate`: 重采样率，单位 Hz（默认: 180）
- `--skip_front`: 跳过开头的样本数（默认: 60）
- `--skip_end`: 跳过末尾的样本数（默认: 60）

**输出**: 在 `csv/` 目录生成处理后的 CSV 文件，每个原始数据目录对应一个 CSV 文件。

---

#### 第二步: 生成统计摘要

```bash
python summary.py
```

**功能**:
- 按 1 秒时间窗口统计速度、距离、加速度等指标
- 生成每个轨迹的详细统计文件（`*_processed.csv`）
- 生成总体摘要文件（`summary.csv`）

**输出**:
- `processed/YYYYMMDD_HHMMSS_processed.csv`: 每秒统计数据
- `processed/summary.csv`: 所有轨迹的汇总统计

---

#### 第三步（可选）: 转换相对位置为绝对位置

```bash
python gt_process.py
```

**功能**:
- 将相对位移转换为基于真实初始位置的绝对坐标
- 默认初始位置为 `[0.0, 0.0, 0.0]`
- 可在 `gt_process.py` 第 53 行修改初始位置

**注意**: 此步骤会直接修改 `csv/` 目录中的 CSV 文件，建议先备份。

---

#### 第四步: 可视化轨迹数据

```bash
python trace_view.py
```

**功能**:
- 读取 `csv/` 目录中的所有 CSV 文件
- 为每个文件生成包含 4 个子图的可视化：
  1. 3D 轨迹图
  2. 2D 平面轨迹图（X-Y 平面）
  3. 全局加速度随时间变化
  4. 航向角（Yaw）随时间变化

**输出**: 显示交互式图像窗口（可手动保存到 `image/` 目录）

---

### 命令速查表

```bash
# 1. 激活环境
conda activate trajectory

# 2. 转换原始数据 → CSV
python to_csv.py

# 3. 生成统计摘要
python summary.py

# 4. （可选）转换为绝对坐标
python gt_process.py

# 5. 可视化
python trace_view.py

# 6. 自定义采样率处理
python to_csv.py --sampling_rate 30

# 7. 退出环境
conda deactivate
```

---

## 输出结果说明

### CSV 文件（`csv/` 目录）

每个原始数据会话对应一个 CSV 文件，包含以下列：

- **时间信息**:
  - `timestamp`: Unix 时间戳（毫秒）
  - `datetime`: 人类可读时间格式（YYYY-MM-DD HH:MM:SS）

- **位置数据**:
  - `pos_x`, `pos_y`, `pos_z`: 3D 位置坐标（米）

- **加速度数据**:
  - `acce_x`, `acce_y`, `acce_z`: 本地坐标系加速度（m/s²）
  - `acce_glob_x`, `acce_glob_y`, `acce_glob_z`: 全局坐标系加速度（m/s²）
  - `linacce_x`, `linacce_y`, `linacce_z`: 线性加速度（m/s²）

- **陀螺仪数据**:
  - `gyro_x`, `gyro_y`, `gyro_z`: 角速度（rad/s）

- **方向数据**:
  - `yaw_degrees`: 航向角（0-360°）
  - `ori_w`, `ori_x`, `ori_y`, `ori_z`: 姿态四元数
  - `rv_w`, `rv_x`, `rv_y`, `rv_z`: 设备朝向四元数

- **其他传感器**:
  - `grav_x`, `grav_y`, `grav_z`: 重力向量（m/s²）
  - `magnet_x`, `magnet_y`, `magnet_z`: 磁力计读数（μT）

---

### 统计文件（`processed/` 目录）

#### 1. 每个轨迹的处理文件 (`*_processed.csv`)

按 1 秒时间窗口聚合的统计数据：

- `时间`: 格式化时间戳（YYYY-MM-DD HH:MM:SS）
- `acce_x_avg`, `acce_y_avg`, `acce_z_avg`: 平均加速度（m/s²）
- `pos_x`, `pos_y`, `pos_z`: 时间窗口结束时的位置（米）
- `平均平面速度`: 水平方向平均速度（m/s）
- `平均3d速度`: 三维空间平均速度（m/s）
- `总平面位移`: 该秒内水平移动距离（米）
- `总3d位移`: 该秒内三维空间移动距离（米）
- `角度`: 航向角（度）

#### 2. 总体摘要文件 (`summary.csv`)

每条轨迹的总体统计：

- `file`: 文件名
- `total_horizontal_distance`: 总水平位移（米）
- `total_time_ms`: 总时长（毫秒）
- `avg_horizontal_speed`: 平均水平速度（m/s）

---

### 可视化输出（`image/` 目录）

运行 `trace_view.py` 后，会为每个 CSV 文件显示包含 4 个子图的可视化窗口：

1. **3D 轨迹图**: 显示行人在三维空间中的运动轨迹
2. **2D 平面轨迹**: 在 X-Y 平面上的投影轨迹
3. **全局加速度时间序列**: 三个方向的加速度随时间变化
4. **航向角时间序列**: 行人朝向角度随时间变化（0-360°）

可手动保存图像到 `image/` 目录。

---

## 常见问题

### Q1: 提示 "pose.txt not found"

**原因**: `raw_data/` 子目录中缺少 `pose.txt` 文件。

**解决方法**:
- 检查数据目录结构是否正确
- 确保每个采集会话的目录都包含 `pose.txt` 文件
- 验证文件名大小写是否正确

---

### Q2: 四元数插值出现 NaN 警告

**现象**: 运行 `to_csv.py` 时出现 "Warning: NaN detected in interpolated quaternion"

**原因**: 四元数插值计算中出现异常值。

**解决方法**:
- 程序会自动用前一个有效值或单位四元数填充，通常不影响结果
- 如果警告过多，检查原始 `pose.txt` 和 `orientation.txt` 数据质量
- 这是正常现象，不影响最终结果

---

### Q3: 提示 "fillna(method='ffill') is deprecated"

**原因**: `trace_view.py` 使用了旧版 Pandas API。

**解决方法**:
- 临时方案: 忽略警告，程序仍可正常运行
- 永久方案: 将 `trace_view.py` 第 34 行修改为：
  ```python
  time_data = time_data.ffill()
  ```

---

### Q4: 环境激活后运行 Python 报错

**检查步骤**:
```bash
# 1. 确认环境激活
conda activate trajectory

# 2. 检查 Python 版本
python --version

# 3. 检查已安装的包
pip list

# 4. 重新安装依赖
pip install -r requirements.txt --force-reinstall
```

---

### Q5: 如何删除和重建环境

```bash
# 退出当前环境
conda deactivate

# 删除环境
conda env remove -n trajectory

# 重新创建环境
conda create -n trajectory python=3.11 -y
conda activate trajectory
pip install -r requirements.txt
```

---

### Q6: Windows 下路径问题

**现象**: 路径包含中文或空格导致错误。

**解决方法**:
- 将项目放在纯英文路径下（如 `D:\projects\3D-Trajectory`）
- 避免路径中包含空格和特殊字符
- 使用 Anaconda Prompt 而非普通 CMD

---

### Q7: 数据处理速度慢

**原因**: 默认采样率较高（180Hz）导致计算量大。

**解决方法**:
```bash
# 使用较低的采样率
python to_csv.py --sampling_rate 30

# 或
python to_csv.py --sampling_rate 60
```

---

### Q8: 图像无法显示

**原因**: 后台运行模式或缺少图形界面。

**解决方法**:
- 确保在有图形界面的环境中运行 `trace_view.py`
- 检查 Matplotlib 后端设置
- 如果在远程服务器，可以修改代码将图像保存为文件而非显示

---

